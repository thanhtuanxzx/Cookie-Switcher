/** @type {import('next').NextConfig} */
const nextConfig = {
  // API routes only - no pages needed
  // This config ensures Next.js API routes work correctly on Vercel
};

export default nextConfig;

